<%
  breadcrumb Book.model_name.human(count: 2) => books_path, @book.image_url => nil
%>
<div class='card'>
  <div class='card-body'>
    <%= button_tag_open_modal edit_book_path(@book), title: t_crud('edit', Book), pull_right: true %>
    <%= detail_view_list @book, *Book::FIELDS %>
  </div>
</div>
