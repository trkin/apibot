<%
  breadcrumb Inspect.model_name.human(count: 2) => nil
%>

<div class='float-left'>
  <%= button_tag_open_modal new_inspect_path  do %>
    <i class="demo-icon icon-plus" aria-hidden="true"></i>
    <%= t_crud('add_new', Inspect) %>
  <% end %>
</div>
<%= @datatable.render_html search_inspects_path(format: :json) %>
