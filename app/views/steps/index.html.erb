<%
  breadcrumb Step.model_name.human(count: 2) => nil
%>

<div class='float-left'>
  <%= button_tag_open_modal new_step_path do %>
    <i class="demo-icon icon-plus" aria-hidden="true"></i>
    <%= t_crud('add_new', Step) %>
  <% end %>
</div>
<%= @datatable.render_html search_steps_path(format: :json) %>
