DEFAULTS: &DEFAULTS
  bot: my_bot
  action: <%= StepService::FIND_AND_CLICK %>
  selector_type: css
  locator: $LABEL locator
  filters: { text: 'Link' }
my_step:
  <<: *DEFAULTS
  position: 0

# step is defined inside each test where it is tested
